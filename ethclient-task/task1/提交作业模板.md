# Task1 提交作业模板

**姓名：**  
**学号/编号：**  
**提交日期：**  

---

## 一、任务 1：区块链读写

### 1.1 完成情况

| 项 | 完成 | 说明 |
|----|:----:|------|
| 环境搭建（Go、go-ethereum、SEPOLIA_RPC_URL） | ☐ | |
| 查询区块：运行 query-block，输出区块信息 | ☐ | |
| 发送交易：运行 send-eth，完成转账并输出交易哈希 | ☐ | |

### 1.2 运行结果

**查询区块（可贴终端输出或截图）：**

```
（在此粘贴 go run ./blockchain-read-write/query-block 的输出）
```

**发送交易（可贴终端输出或截图，注意脱敏私钥/地址）：**

```
（在此粘贴 go run ./blockchain-read-write/send-eth 的输出或 Etherscan 链接）
```

---

## 二、任务 2：合约代码生成

### 2.1 完成情况

| 项 | 完成 | 说明 |
|----|:----:|------|
| 编写 Counter.sol 并编译得到 ABI/BIN | ☐ | |
| 使用 abigen 生成 Go 绑定（contract/counter.go） | ☐ | |
| 部署合约：运行 deploy-counter，获得合约地址 | ☐ | |
| 调用合约：运行 call-counter，完成 increment 并输出 count | ☐ | |

### 2.2 运行结果

**部署合约（可贴终端输出或截图）：**

```
（在此粘贴 go run ./contract-codegen/deploy-counter 的输出，含合约地址与部署耗时）
```

**调用合约（可贴终端输出或截图）：**

```
（在此粘贴 go run ./contract-codegen/call-counter 的输出，含当前 count、交易哈希、调用后 count、交易耗时）
```

**合约地址（Sepolia）：** `（填写你部署的 Counter 合约地址）`

---

## 三、备注

（选填：遇到的问题、参考链接、环境说明等）

---

*提交时可将本文件重命名为「姓名_Task1_提交.md」或按课程要求命名。*
